package models.sso;

/**
 * User sign in state.
 */
public enum UserSignInState {

    /**
     * Sign in is enabled (default).
     */
    ENABLED,

    /**
     * Sign in is disabled.
     */
    DISABLED;
}
